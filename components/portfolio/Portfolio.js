import React from 'react';
import { COLORS } from '../../styles';

const Portfolio = () => {
  return (
    <div
      id="portfolio"
      style={{ height: 800, backgroundColor: COLORS.backgroundColor }}
    ></div>
  );
};

export default Portfolio;
